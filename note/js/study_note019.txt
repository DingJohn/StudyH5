1.数组的创建
    1.var arr = new array();
    2.var arr = [];
    3.声明了数组并给了数组长度但是没有赋值，则数组中每个值为undefined
2.length
    1.arr.length();获取数组的长度
    2.arr.length=2;改变数组的长度
3.查找数组中的最大值或最小值，在定义最大值或最小值是，不能初始化为0，否则可能会出bug，最好使用数组第一个下标