1.switch
    1.在比较值的时候使用的是全等操作符，因此不会发生类型转换（如“10”不等于数值10）
2.do-while
    1.首先执行一次do中的循环体，再判断while中的条件，如果条件满足则继续执行do中的循环体
3.prompt()
    1.接收用户的输入
4.document.write()
    1.在网页中显示要输出的内容，一般是html代码
5.调试操作
    1.watch：监视，通过watch查看变量值的变化
    2.F10：单步执行
    3.F8：调到下一断点，如没有断点，则结束执行